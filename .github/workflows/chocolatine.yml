name: Chocolatine
on: [push, pull_request]

jobs:
    check_compilation_and_coding_style:
        runs-on: ubuntu-latest
        # runs-on: epitechcontent/epitest-docker
        steps:
          - name: checkout repository
          - run:
            - uses: actions/checkout@v5
        steps:
          - run: ls -la
        steps:
          - run: grep main
        # steps:
        #   - name: compilation
        #   - run: make | annotate-errors.sh
        # steps:
        #   - name: banana-check-repo
        #   - run:
        # steps:
        #   - name: clean files
        #   - run: make clean
